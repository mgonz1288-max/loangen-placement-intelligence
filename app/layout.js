import './styles.css';

export const metadata = {
  title: 'Placement Intelligence | loanGen',
  description: 'Internal placement strategy prototype',
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
